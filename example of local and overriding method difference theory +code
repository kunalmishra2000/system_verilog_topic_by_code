  // so local varaibel and method not accessable/visiable/override  outside of base class 
//local blocks overriding

/*A local method or variable:

Cannot be overridden

Is invisible to child classes

Is inaccessible outside its own class

Prevents polymorphism*/

class packet;
  local virtual function post();
    $display("[base]");
  endfunction
endclass

class child extends packet;
  function post();        
    $display("[child]");
  endfunction
endclass

module tab;
  packet base;
  child pac = new();  
  initial begin

    base=pac;
    base.post();
  
  end
endmodule

/*Rules for local:

Accessible only inside packet

❌ Not accessible from module

❌ Not accessible from child

❌ Not accessible via base handle
*/
